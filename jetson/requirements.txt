# Jetson Gateway API dependencies

fastapi[standard]>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
httpx>=0.27,<1.0
pydantic>=2.8,<3.0
numpy>=1.26,<3.0
xArm-Python-SDK

# OpenCV and pyrealsense2 are **not installed via pip** on Jetson.

# They must be installed manually:

# - RealSense SDK → built from source (see instructions above)
# - OpenCV → install using the Jetson optimized guide https://qengineering.eu/install-opencv-on-orin-nano.html